# Tiny Layered App

This fixture is a minimal TypeScript-style layered application used for
deterministic inspector end-to-end tests.

The app separates domain, application, and adapter code so repository indexing
can observe a tiny layered structure.
