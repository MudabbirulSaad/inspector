# Tiny Node App

This fixture is a minimal Node.js application used for deterministic inspector
end-to-end tests.

It exposes a greeting function from `src/index.js` and protects that behavior
with a small Node test.
