Revision rules:

- When a revision request is present, address each failed check directly.
- Preserve valid evidence from earlier outputs when it still applies.
- Replace unsupported claims instead of restating them.
- Explain any correction through the requested output contract, not free-form commentary.
- If the revision request conflicts with repository evidence, follow the evidence and make the conflict explicit.
